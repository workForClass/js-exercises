// 8) Declare and initialise an single-dimensional array with 5 names. Log the table to the console.

let name = "Lavina"; // a single value stored in a variable

let namesArray = ["Denise", "Nam", "Yuliia", "Matheson", "Shifnas"] // 1 single dimension array  stored in a variable

 console.table(namesArray); // logs table of items & index
// console.log(namesArray[0]); // logs array item by index number

 // Multidimensional (two-dimensional) Array  stored in a variable
let fruitArray = [
    ['apple', 'orange', 'pear'],
    ['carrots', 'beans', 'peas'],
    ['cookies', 'cake', 'muffins', 'pie']
];

// console.table(fruitArray); // logs table of two-dimensional array of arrays.