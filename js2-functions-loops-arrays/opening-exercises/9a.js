// 9) Use a for loop to loop through the array and log the array index and values
let namesArray = ["Denise", "Nam", "Yuliia", "Matheson", "Shifnas"];

for (i = 0; i < namesArray.length; i++) {
  console.log(`${namesArray[i]} is in position ${i}`);
}

// console.log(namesArray[0][0]); // 
